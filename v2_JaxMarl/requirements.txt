# Tested from the local `jax` conda env with Python 3.10.
# Create a fresh env first, then install with:
#   pip install -r requirements.txt

jax==0.6.2
jaxlib==0.6.2
flax==0.10.7
optax==0.2.7
chex==0.1.90
distrax==0.1.5
jaxmarl==0.0.7
wandb==0.25.1
numpy==1.26.4
pillow==12.1.1
matplotlib==3.10.8
